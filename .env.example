# Optional. If unset, the app auto-picks:
#   - http://localhost:3001 during `npm run dev` (your local json-server)
#   - https://my-json-server.typicode.com/Saikiran1603/CM-Chat-App in production builds
#
# Set this only if you're pointing at your own hosted json-server instead
# (e.g. Render, Railway) for real write persistence in production.
# VITE_API_URL=https://your-hosted-json-server.example.com
